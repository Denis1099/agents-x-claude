# Apify token — https://console.apify.com/account/integrations
APIFY_TOKEN=

# Framer project the demo builds into. Create once with:
#   npx @framer/agent@latest project new    (requires one browser approval)
FRAMER_PROJECT_ID=
